import { css } from "@emotion/react";

const andiuiBtn = css`
	background-color: green;
`;

const cardCss = {
	self: css({
		backgroundColor: "white",
		border: "1px solid #eee",
		borderRadius: "0.5rem",
		padding: "1rem",
	}),

	title: css({
		fontSize: "1.25rem",
	}),
};

export { andiuiBtn };

import { Canvas, Meta } from "@storybook/blocks";

import * as ButtonStories from "./button.stories";

<Meta of={ButtonStories} title="" />

# button

A button is a square box that can be activated or deactivated when ticked.

Use buttones to select one or more options from a list of choices.

<Canvas of={ButtonStories.Primary} />
